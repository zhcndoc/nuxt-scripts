---
title: <ScriptLeafletGeoJson>
---

通过 `L.geoJSON` 添加内联 GeoJSON。替换 `data` 会清空并重新填充现有图层；更改 `options.style` 会更新其路径样式。

::script-types{script-key="leaflet" filter="ScriptLeafletGeoJson"}
::

远程获取有意被排除在组件之外。使用 `useFetch` 获取并验证数据，然后将生成的对象传递给 `data`，以便加载和失败状态保持明确。
