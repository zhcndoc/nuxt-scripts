---
title: v0 到 v1
description: 从 Nuxt Scripts v0.x 升级到 v1.0 的迁移指南。
---

## v1 新特性

v1 专注于**隐私**和**性能**：通过你自己的域名路由分析脚本，将脚本移出主线程，并在服务器端渲染社交嵌入内容。

### 第一方模式 ([#577](https://github.com/nuxt/scripts/pull/577))

第三方脚本会暴露数据，使得能够跨站点对用户进行指纹识别。每个请求都会共享用户的 IP 地址，而且脚本可以设置第三方 Cookie 用于跨站点跟踪。不同的提供商侵入性不同，例如，X Pixel 会访问 9 个浏览器指纹 API（包括 `navigator.getBattery()`{lang="ts"}），设置 5 个跟踪 Cookie，并向 3 个独立的域名发送数据。甚至 Microsoft Clarity 也会在 3 个域名上读取 10 个指纹 API。

第一方模式通过你自己的域名路由所有脚本流量。对于支持它的脚本，这是**自动启用**的，无需配置：

```ts
export default defineNuxtConfig({
  scripts: {
    registry: {
      googleAnalytics: { id: 'G-XXXXXX' },
      metaPixel: { id: '123456' },
    }
  }
})
```

- **用户 IP 保持私密**：第三方看到的是你服务器的 IP
- **无第三方 Cookie**：请求是同源的
-**适用于广告拦截器**：请求显示为第一方
- 按脚本选择退出：`proxy: false`
- 全局禁用：`proxy: false`

支持的脚本：Google Analytics、GTM、Meta Pixel、TikTok、Segment、Clarity、Hotjar、X/Twitter、Snapchat、Reddit。

详情参见[第一方指南](/docs/guides/first-party)。

### Partytown 支持 ([#576](https://github.com/nuxt/scripts/pull/576))

使用 Web Workers 将脚本加载到主线程之外。为每个脚本设置 `partytown: true`：

```ts
export default defineNuxtConfig({
  modules: ['@nuxtjs/partytown', '@nuxt/scripts'],
  scripts: {
    registry: {
      plausibleAnalytics: { domain: 'example.com', partytown: true },
      fathomAnalytics: { site: 'XXXXX', partytown: true },
    }
  }
})
```

转发数组会为支持的脚本自动配置。

::callout{icon="i-heroicons-exclamation-triangle" color="amber"}
GA4 与 Partytown 存在[已知问题](https://github.com/BuilderIO/partytown/issues/583)。GTM 不兼容。请考虑使用 Plausible、Fathom 或 Umami 替代。
::

### SSR 社交嵌入 ([#590](https://github.com/nuxt/scripts/pull/590))

在服务器端渲染 X、Instagram 和 Bluesky 嵌入内容，无需加载第三方 JavaScript。

在你的 `nuxt.config` 中启用你需要的嵌入：

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  scripts: {
    registry: {
      xEmbed: true,
      instagramEmbed: true,
      blueskyEmbed: true,
    },
  },
})
```

```vue
<ScriptXEmbed tweet-id="1754336034228171055">
  <template #default="{ userName, text, likesFormatted }">
    <!-- 通过作用域插槽完全控制样式 -->
  </template>
</ScriptXEmbed>

<ScriptInstagramEmbed post-url="https://instagram.com/p/ABC123/">
  <template #default="{ html }">
    <div v-html="html" />
  </template>
</ScriptInstagramEmbed>

<ScriptBlueskyEmbed post-url="https://bsky.app/profile/...">
  <template #default="{ html }">
    <div v-html="html" />
  </template>
</ScriptBlueskyEmbed>
```

- 零第三方 JavaScript
- X/Instagram/Bluesky 不设置 Cookie
- 不共享用户 IP
- 所有内容都从你的域名提供

### 同意触发器撤销 ([#631](https://github.com/nuxt/scripts/pull/631))

`useScriptTriggerConsent()`{lang="ts"} 组合式函数现在支持在运行时撤销同意：

```ts
const trigger = useScriptTriggerConsent()
useScriptGoogleTagManager({ id: 'GTM-XXX', scriptOptions: { trigger } })

trigger.accept() // 授予同意
trigger.revoke() // 撤销同意
trigger.consented // Ref<boolean>
```

### 脚本重载 ([77f853b](https://github.com/nuxt/scripts/commit/77f853b))

在 SPA 导航后重新执行扫描 DOM 的脚本：

```ts
const script = useScript('/third-party.js')
await script.reload()
```

### SRI 完整性哈希 ([#575](https://github.com/nuxt/scripts/pull/575))

```ts
export default defineNuxtConfig({
  scripts: {
    assets: {
      integrity: 'sha384' // 或 'sha256'、'sha512'
    }
  }
})
```

### 脚本统计信息导出

新的 `@nuxt/scripts/stats` 子路径导出，用于审计脚本的隐私、性能和安全性特征。

```ts
import { getScriptStats } from '@nuxt/scripts/stats'

const stats = await getScriptStats()
// 隐私评级（A+ 到 F）、性能评级、CWV 估计
```

### GTM 默认同意 ([#544](https://github.com/nuxt/scripts/pull/544))

```ts
export default defineNuxtConfig({
  scripts: {
    registry: {
      googleTagManager: {
        id: 'GTM-XXXX',
        defaultConsent: {
          ad_storage: 'denied',
          analytics_storage: 'denied'
        }
      }
    }
  }
})
```

### 新的注册表脚本

- **PostHog** ([#568](https://github.com/nuxt/scripts/pull/568))：产品分析与功能标志
- **Google reCAPTCHA v3** ([#567](https://github.com/nuxt/scripts/pull/567))：隐形机器人防护
- **TikTok Pixel** ([#569](https://github.com/nuxt/scripts/pull/569))：转化跟踪
- **Google Sign-In** ([#573](https://github.com/nuxt/scripts/pull/573))：一键认证
- **Bing UET** ([#650](https://github.com/nuxt/scripts/pull/650))：微软广告转化跟踪
- **Mixpanel Analytics** ([#648](https://github.com/nuxt/scripts/pull/648))：产品分析与用户跟踪
- **Vercel Analytics** ([#605](https://github.com/nuxt/scripts/pull/605))：Vercel Web Analytics 集成
- **Gravatar** ([#606](https://github.com/nuxt/scripts/pull/606))：具有隐私保护代理的头像服务

### Vimeo 播放器增强 ([#624](https://github.com/nuxt/scripts/pull/624))

新的 `ratio` 属性用于控制宽高比，与 YouTube 播放器 API 保持一致。

### 谷歌地图配色模式 ([#587](https://github.com/nuxt/scripts/pull/587))

```vue
<ScriptGoogleMaps
  :map-ids="{ light: 'LIGHT_MAP_ID', dark: 'DARK_MAP_ID' }"
/>
```

随 `@nuxtjs/color-mode` 自动切换，或通过手动设置 `color-mode` 属性。

### 谷歌地图地理编码代理

服务器端地理编码代理，用于降低计费成本并隐藏 API 密钥。当 `googleMaps` 在你的注册表中时自动启用。

## 注册表配置变更

v1 重新设计了注册表条目的工作方式。关键变化：**存在即启用基础设施，`trigger` 启用自动加载**。

### 配置迁移

| v0 | v1 | 变更内容 |
|----|-----|--------------|
| `googleAnalytics: true` | `googleAnalytics: { id: 'G-xxx', trigger: 'onNuxtReady' }` | `true` 是 `{ trigger: 'onNuxtReady' }` 的别名（自动加载）；使用 `{}` 仅启用基础设施 |
| `googleAnalytics: 'proxy-only'` | `googleAnalytics: { id: 'G-xxx' }` | `'proxy-only'` 已移除；现在仅启用基础设施是默认值 |
| `[{ id: '...' }, { reverseProxyIntercept: false }]` | `{ id: '...', proxy: false }` | `reverseProxyIntercept` 重命名为 `proxy`；首选扁平语法 |
| `[{ id: '...' }, { bundle: true }]` | `{ id: '...' }` | 打包通过能力自动启用；无需主动选择 |
| `googleAnalytics: 'mock'` | `googleAnalytics: 'mock'` | 未变更；创建测试存根 |

### 扁平配置语法

v1 支持扁平配置语法。`trigger`、`proxy`、`bundle` 和 `partytown` 可以在顶层设置：

```ts [v0 nuxt.config.ts]
export default defineNuxtConfig({
  scripts: {
    registry: {
      googleAnalytics: [{ id: 'G-xxx' }, { trigger: 'onNuxtReady' }],
      plausibleAnalytics: [{ domain: 'mysite.com' }, { proxy: false }],
    }
  }
})
```

```ts [v1 nuxt.config.ts]
export default defineNuxtConfig({
  scripts: {
    registry: {
      googleAnalytics: { id: 'G-xxx', trigger: 'onNuxtReady' },
      plausibleAnalytics: { domain: 'mysite.com', proxy: false },
    }
  }
})
```

### 环境变量

注册表配置字段通过 Nuxt 标准的 `runtimeConfig.public` 机制解析。将你的脚本配置添加到 `runtimeConfig.public.scripts` 下，并通过环境变量引用：

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  runtimeConfig: {
    public: {
      scripts: {
        googleAnalytics: { id: '' }, // NUXT_PUBLIC_SCRIPTS_GOOGLE_ANALYTICS_ID
        posthog: { apiKey: '' }, // NUXT_PUBLIC_SCRIPTS_POSTHOG_API_KEY
      }
    }
  }
})
```

### 使用 `'mock'` 进行测试

使用 `'mock'` 注册脚本存根而不加载任何内容。这会创建基础设施（类型、组合式函数自动导入），设置 `trigger: 'manual'` 并禁用验证：

```ts [nuxt.config.ts]
export default defineNuxtConfig({
  scripts: {
    registry: {
      googleAnalytics: 'mock',
    }
  }
})
```

## 重大变更

### PayPal SDK v6 ([#628](https://github.com/nuxt/scripts/pull/628))

已迁移到 PayPal JavaScript SDK v6。组件 API 发生了重大变化：

- SDK URL 现在使用 v6 端点（`/web-sdk/v6/core`）
- 新的认证模式：`clientId`（可选 `clientToken`）或单独使用 `clientToken`
- 沙盒模式在开发环境中默认设为 `true`

### YouTube 播放器

#### 宽高比

使用 `ratio` 属性代替从 `width/height` 推导：

```diff
<ScriptYouTubePlayer
  video-id="..."
-  :width="1280"
-  :height="720"
+  ratio="16/9"
/>
```

默认为 `16/9`。

#### 占位图片

默认的 `object-fit` 从 `contain` 改为 `cover`：

```vue
<ScriptYouTubePlayer video-id="..." placeholder-object-fit="contain" />
```

#### 多个播放器

播放器实例现在已正确隔离。移除任何针对多个播放器的变通方案。

### 谷歌标签管理器

#### onBeforeGtmStart 回调

现在会为缓存的/预初始化的脚本触发。防止多次调用：

```ts
let initialized = false
useScriptGoogleTagManager({
  onBeforeGtmStart: (gtag) => {
    if (initialized)
      return
    initialized = true
    // 你的初始化代码
  }
})
```

### 类型扩充

模板已重新组织。升级后运行 `nuxi prepare`。
